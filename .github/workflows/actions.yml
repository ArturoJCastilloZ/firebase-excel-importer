name: Firebase excel importer

on:
  push:
    branches:
      - master

  schedule:
    - cron: '0 5 * * *' # Todos los días a las 5:00 am

jobs:
  build:
    runs-on: ubuntu-latest
    environment: 
      name: Production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.11.3'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run project
        run: |
          python main.py \
            --bucket ${{ VARS.BUCKET }} \
            --blob ${{ VARS.BLOB }} \
            --path ${{ VARS.PATH_FILE }} \
            --destination ${{ VARS.DESTINATION }} \
            --type ${{ VARS.TYPE }} \
            --project-id ${{ VARS.PROJECT_ID }} \
            --private-key-id ${{ VARS.PRIVATE_KEY_ID }} \
            --private-key "${{ VARS.PRIVATE_KEY }}" \
            --client-email ${{ VARS.CLIENT_EMAIL }} \
            --client-id ${{ VARS.CLIENT_ID }} \
            --auth-uri ${{ VARS.AUTH_URI }} \
            --token-uri ${{ VARS.TOKEN_URI }} \
            --auth-provider-x509-cert-url ${{ VARS.AUTH_PROVIDER_X509_CERT_URL }} \
            --client-x509-cert-url ${{ VARS.CLIENT_X509_CERT_URL }}

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git diff-index --quiet HEAD || (git commit -a -m "updated logs" --allow-empty)

      - name: Push changes
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
